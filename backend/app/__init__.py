from dotenv import load_dotenv
load_dotenv()

import logging
logging.basicConfig(level=logging.INFO)

# from .core.pipel
# You could import and expose key parts here if needed:ine import run_daily_pipeline
